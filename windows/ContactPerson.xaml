<Window x:Class="TuwenDayinDian.windows.ContactPerson"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TuwenDayinDian.windows"
        mc:Ignorable="d"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Height="450" Width="362"
        Background="Transparent" 
        WindowStyle="None" 
        OpacityMask="White"
        AllowsTransparency="True" 
    WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
      UseLayoutRounding="False">
   

    <Border Grid.Row="0"
                Width="362"
                Height="450"                  
                Background="White"
                CornerRadius="20"
                BorderThickness="1"
                BorderBrush="Gray">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="2"/>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <DockPanel  Grid.Row="0"   Width="314" LastChildFill="False">
                <Label DockPanel.Dock="Left" Foreground="#3f3f45" FontSize="16" FontWeight="Bold" Content="对接人" Height="30" />
                <Button DockPanel.Dock="Right"  Width="30" Height="30" 
                Background="Transparent"
                BorderBrush="Transparent"
                Foreground="#c0c0c2"
                FontSize="25"
                Content="×"                       
                Click="close_setPrintParam"/>
            </DockPanel>
            <Border Grid.Row="1"  Width="314" Height="2" Background="#f3f3f3"  />


            <StackPanel Name="textBoxList" Grid.Row="2" Width="300"  Orientation="Vertical"  HorizontalAlignment="Center" >
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人1"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人2"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人3"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人4"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人5"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人6"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人7"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人8"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox  Width="285" Height="35" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"  BorderThickness="0,0,0,1"  FontSize="15" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left" AlignmentY="Bottom">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="对接人9"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>




                <!--<ListBox x:Name="BindingMethodListBox"
              SelectionMode="Multiple"
                  BorderBrush="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding}"
                           IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, 
                         Path=IsSelected, Mode=TwoWay}" />
                        </DataTemplate>
                        
                    </ListBox.ItemTemplate>
                    
                    --><!-- 示例数据，可以根据需求修改 --><!--
                    <sys:String>胶装</sys:String>
                    <sys:String>骑马钉</sys:String>
                    <sys:String>压痕</sys:String>
                    <sys:String>覆膜</sys:String>
                    <sys:String>线圈</sys:String>
                    <sys:String>胶条</sys:String>

                    --><!-- ... 其他装订方式 --><!--
                </ListBox>-->

            </StackPanel>





            <Button Content="保存"
                    FontSize="15"
                    FontWeight="Black"  
                    Width="314"
                    Height="48"
                    Grid.Row="4"
                    VerticalAlignment="Top"
                    Click="GetAllValueStr"
                    Style="{StaticResource BootstrapButtonYellow}" Margin="0,0,0,0" />
        </Grid>
    </Border>
</Window>


